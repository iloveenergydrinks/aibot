# AI and LLM
openai>=1.40.0
anthropic>=0.34.0

# Discord Integration
discord.py[voice]>=2.3.2
PyNaCl>=1.5.0
# Note: discord-ext-voice-recv uses alpha releases
# Install manually with: python -m pip install discord-ext-voice-recv --pre

# Audio Processing
SpeechRecognition>=3.10.0
pydub>=0.25.1
soundfile>=0.12.1
numpy>=1.24.0
pyaudio>=0.2.14
# Note: simpleaudio requires C++ build tools on Windows - optional

# TTS Options
elevenlabs>=1.5.0
gtts>=2.5.0

# Utilities
python-dotenv>=1.0.0
aiohttp>=3.9.0
asyncio>=3.4.3

# Browser Automation for Cloud VM
selenium>=4.15.0
undetected-chromedriver>=3.5.0
webdriver-manager>=4.0.0

# Voice Activity Detection
# Note: webrtcvad requires C++ build tools on Windows - optional

# Web API (deprecated - using Firebase instead)
# flask>=3.0.0
# flask-cors>=4.0.0

# Firebase for centralized stats
firebase-admin>=6.0.0
requests>=2.31.0
